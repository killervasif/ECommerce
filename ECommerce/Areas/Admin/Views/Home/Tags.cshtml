@model List<Tag>

@{
    Layout = "_LayoutAdmin";
}


<div class="col-12">
    <div class="bg-secondary rounded h-100 p-4">
        <h6 class="mb-4">Hoverable Table</h6>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var t in Model)
                {
                    <tr>
                        <th scope="row">@t.Id</th>
                        <td>@t.Name</td>
                        <td>
                            <span>
                                <a class="btn btn-link" href="@Url.Action("EditTag","Home",new {t.Id})">Edit</a>
                                <a class="btn btn-link" href="@Url.Action("DeleteTag","Home",new {t.Id})">Delete</a>
                            </span>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
