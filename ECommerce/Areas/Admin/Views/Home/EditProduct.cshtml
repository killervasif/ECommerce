@model AddProductViewModel

@{
    Layout = "_LayoutAdmin";
}

<div class="col-12">
    <div class="bg-secondary rounded h-100 p-4">
        <form asp-action="UpdateProduct" asp-controller="Home" asp-route-model="@Model" asp-route-id="@ViewBag.Id" method="post" enctype="multipart/form-data">
            <label for="name" class="form-label">Name</label>
            <input asp-for="@Model.Name" class="form-control" id="name" />
            <span asp-validation-for="@Model.Name" class="text-danger"></span>
            <label for="description" class="form-label">Description</label>
            <textarea asp-for="@Model.Description" class="form-control" id="description"></textarea>
            <span asp-validation-for="@Model.Description" class="text-danger"></span>
            <label for="image" class="form-label">Image</label>
            <input type="file" asp-for="@Model.ImageUrl" class="form-control mb-2 bg-dark" itemid="image" />
            <span asp-validation-for="@Model.ImageUrl" class="text-danger"></span>
            <label for="categoryId" class="form-label">Category</label>
            <select asp-for="@Model.CategoryId" class="form-select mb-2" asp-items="@ViewBag.Categories" id="categoryId"></select>
            <span asp-validation-for="@Model.CategoryId" class="text-danger"></span>
            <label for="tagIds" class="form-label">Tags</label>
            <select multiple style="width:350px" class="form-select mb-2" asp-for="@Model.TagIds" asp-items="@ViewBag.Tags" id="tagIds"></select>
            <span asp-validation-for="@Model.TagIds" class="text-danger"></span>
            <button type="submit" class="btn btn-primary">Accept</button>
        </form>
    </div>
</div>